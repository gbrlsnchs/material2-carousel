export interface MatCarousel {
  timings: string;
  loop: boolean;
  autoplay: boolean;
  autoplayInterval: number;
  showArrows: boolean;
  showIndicators: boolean;
  maxWidth: string;
  proportion: number;
  maxSlides: number;
  color: string;
  useMouseWheel: boolean;
  useKeyboard: boolean;
  rtl: boolean;
}
