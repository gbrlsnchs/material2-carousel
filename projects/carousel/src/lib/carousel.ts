export interface MatCarousel {
  timings?: string;
  loop?: boolean;
  autoplay?: boolean;
  autoplayInterval?: number;
  showArrows?: boolean;
  showIndicators?: boolean;
  awaitAnimation?: boolean;
  maxSlides?: number;
}
