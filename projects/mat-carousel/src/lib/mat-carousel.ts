export interface MatCarousel {
  timings?: string;
  loop?: boolean;
  autoplay?: boolean;
  autoplayInterval?: number;
  showArrows?: boolean;
  showStepper?: boolean;
  awaitAnimation?: boolean;
  maxItems?: number;
}
